# Server configuration
server.port=9200

# Application name
spring.application.name=java-calculator

# Optional: Enable actuator for cloud health checks / liveness probes
management.endpoints.web.exposure.include=health,info
