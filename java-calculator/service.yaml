apiVersion: v1
kind: Service
metadata:
  name: calculator-service
spec:
  type: NodePort
  selector:
    app: java-calculator
  ports:
    - protocol: TCP
      port: 9200
      targetPort: 9200
      nodePort: 30092
